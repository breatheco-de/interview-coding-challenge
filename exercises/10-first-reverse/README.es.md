# `10` Invertir cadena

Haga que la función `FirstReverse(str)` tome el parámetro `str` y devuelva la cadena en orden inverso. Por ejemplo: si la cadena de entrada es "Hello World and Coders" entonces su programa debería devolver "sredoC dna dlroW olleH".

## Ejemplos

```
Input: "coderbyte"
Output: etybredoc
```

```
Input: "I Love Code"
Output: edoC evoL I
```
